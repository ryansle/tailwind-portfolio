/**
 * Site-wide values that appear in more than one place. The contact address in
 * particular used to be written out per-component, which let the contact page
 * and the contact form drift onto two different inboxes.
 */

const contactEmail = 'ryanle@live.com';
const contactEmailHref = `mailto:${contactEmail}`;

export { contactEmail, contactEmailHref };
